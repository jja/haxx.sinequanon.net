<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ sitetitle }} - {{ title }}</title>
  <meta name="description" content="{{ sitedescription }}">
  <link rel="stylesheet" type="text/css" href="/css/site.css" />
</head>
<body>

  <header>
    <h1>
      <span class="header-span">
        <a style="color: black; text-decoration: none;" rel="home" href="{{ siteurl }}">{{ sitetitle }}</a>
      </span>
    </h1>
  </header>

  <article>

      <h1>{{ title }}</h1>

      <section id="meta">

          <div id="byline">
            by {{ author }}
            on <time datetime="{{>datetime}}">{{>date}}</time>
          </div>

          Topics: <ul id="tags">
            {{#tags as |tag|}}
              <li class="tagitem">
                <a class="tag" href="/tag/{{ tag }}">{{ tag }}</a>
              </li>
            {{/tags}}
          </ul>

      </section>

      <section id="post_content">

        {{{ contents }}}

      </section>

  </article>

  <div id="comment-form">
    {{#if comments}}
    {{else}}
        (comments are closed)
    {{/if}}
  </div>

  <section id="nav">
    {{#if previous}}
      <span class="footer-span">
        Previous:
        <a class="plain" href="/{{previous.path}}">{{previous.title}}</a>
      </span>
    {{/if}}

    {{#if next}}
      <span class="footer-span">
        Next:
        <a class="plain" href="/{{next.path}}">{{next.title}}</a>
      </span>
    {{/if}}
  </section>

  <footer>
    <span class="footer-span">
        <a class="plain" rel="home" href="{{ siteurl }}">Home</a>
    </span>
  </footer>

</body>
</html>
