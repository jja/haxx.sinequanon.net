<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ sitetitle }} - {{ tag }}</title>
  <meta name="description" content="{{ sitedescription }}">
  <link rel="stylesheet" type="text/css" href="/css/site.css" />
</head>
<body>

  <header>
    <h1>
      <span class="header-span">
        <a style="color: black; text-decoration: none;" rel="home" href="{{ siteurl }}">{{ sitetitle }}</a>
      </span>
    </h1>
  </header>

  <h1>Posts for {{tag}}</h1>

  {{#pagination.files as |file|}}
    <a href="/{{file.path}}">{{ file.title }}</a>
    excerpt: {{{ file.less }}}

  {{/pagination.files}}

  {{#if pagination.previous}}
    <a href="/{{pagination.previous.path}}">Newer</a>
  {{/if}}

  {{#if pagination.next}}
    <a href="/{{pagination.next.path}}">Older</a>
  {{/if}}

</body>
</html>
